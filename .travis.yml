language: go
go:
  - 1.8.x
  - 1.9.x
  - 1.10.x
  - tip
services:
  - redis-server
env:
  - GAE=.go_appengine
  - GAE=
cache:
  directories:
    - vendor
    - glide-$(go version | awk '{ print $NF }' | tr '/' '-')
    - .download
    - .go_appengine
install:
  - make installdeps
script:
  - make test
